/* A program to perform Euclid's
   Algorithm to computer gcd */

int gcd (int u, int v)
{
	if (v == 0) return u;
	else return output(u);
	/* u-u/v*v == u mod v */
}


void main(void)
{
	int x; int y;
	int x;
	x = input(); y = input();
	output(gcd(x,y));
	z = input();
}
